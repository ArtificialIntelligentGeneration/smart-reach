# Telegram Sender – руководство по форматированию

Этот файл фиксирует финальное решение проблемы «теряющихся» отступов и форматирования в Telegram, а также описывает, как оно реализовано в коде.

---
## Что было
1. HTML-сообщения, экспортированные из `QTextEdit`, содержали CSS и теги, которые **Telegram Bot API** «чистил».  
2. Одиночные `<br>` в начале/между тегами выкидывались, из-за чего абзацы «склеивались».  
3. Попытки дублировать `<br>` или добавлять нулевой пробел решали отступы, но ломали форматирование.

## Итоговая стратегия
Мы ушли от HTML и перешли на **Markdown** (legacy-диалект Pyrogram).

1. В `main.py` добавлен конвертер `html_to_md()` (см. начало файла).  
   * До парсинга заменяет `<span style="font-weight…">` → `<b>`, `<span style="font-style:italic">` → `<i>`.
   * С помощью **BeautifulSoup** рекурсивно пробегает DOM и генерирует Markdown:
     * `<b>/<strong>` → `**text**`
     * `<i>/<em>` → `__text__`
     * `<a href>`   → `[text](url)`
     * `<br>` и `<p>` → `\n` / `\n\n`
   * «Голый» текст экранируется функцией `_escape_md`, чтобы спец-символы `\ * _ \` [ ] ( )` не поломали разметку (знак «!» больше не экранируется).
2. `BroadcastWorker` формирует сообщение так:
   ```python
   self.message = html_to_md(raw_html)
   client.send_message(chat_id, self.message, parse_mode=enums.ParseMode.DEFAULT)
   ```
   Режим `DEFAULT` позволяет Markdown и HTML одновременно, но мы передаём только Markdown.
3. UI-редактор «Скрипты» остаётся WYSIWYG-HTML (жирный/курсив/ссылки), но при сохранении мы записываем **сырой HTML**.  Сам конверт происходит незаметно перед отправкой.

## Как пользоваться
1. Вкладка «Скрипты»
   * Создайте или отредактируйте скрипт.  Кнопки **B**, *I*, 🔗 работают как обычно.
   * Для абзаца – просто Enter; пустая строка — двойной Enter.
   * Фиксированные отступы можно задать через блок кода:
     ```html
     <pre>
       два пробела остаются
     </pre>
     ```
2. Вкладка «Рассылка»
   * Выберите скрипт в комбо-боксе.
   * Отметьте аккаунты + введите получателей (по одному на строку).
   * Задайте задержки и жмите «Запустить».
3. Логи каждой рассылки сохраняются в `broadcast_logs/` (HTML-файл).

## Зависимости
```
pip install pyrogram beautifulsoup4 nest_asyncio
```

## Где искать код
* Конвертер Markdown    – `main.py`, строки ~20-90  
* Поток рассылки        – `BroadcastWorker` (`main.py`, строки 300+)  
* Редактор скриптов     – `setup_scripts_tab()`

---
Версия зафиксирована 21 июня 2025. 