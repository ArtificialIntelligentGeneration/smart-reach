### ТЗ: Этап 4 — Антиспам и устойчивость отправки

- **Цель**: сделать Desktop устойчивым к антиспам-ограничениям Telegram, добавить интеграцию с @SpamBot, реализовать стратегии пауз для PeerFlood, улучшить обработку FloodWait и обеспечить корректное возобновление рассылки после прерываний.
- **Срок**: 1 неделя
- **Зависимости**: базовая логика рассылки готова (этап 2), рабочий Desktop с интеграцией лицензий (этап 3).
- **Исключено**: биллинг, backend изменения, ChatPicker, OpenAI.

### Объём работ (Scope)

#### Антиспам механизмы
- Реализовать стратегию пауз для PeerFlood: аккаунт уходит в паузу на N минут (конфигурируемо, по умолчанию 30-60 мин)
- Добавить опциональную интеграцию с @SpamBot для автоматического снятия ограничений
- Улучшить обработку FloodWait: адаптивные паузы, экспоненциальный backoff
- Добавить UI настройки антиспама в вкладку «Рассылка»

#### Устойчивость отправки
- Реализовать возобновление рассылки с того же места после пауз/ошибок
- Сохранять прогресс рассылки (отправленные сообщения по аккаунтам)
- Добавить логику перераспределения нагрузки при выпадении аккаунтов
- Обеспечить корректное завершение/откат лицензионных резервов при прерываниях

#### Интеграция @SpamBot
- Автоматический запуск @SpamBot при PeerFlood
- Мониторинг статуса снятия ограничений
- Конфигурируемые задержки и повторные попытки

### Требования (функциональные)

#### PeerFlood обработка
- При PeerFlood: аккаунт помечается как "в паузе" на X минут (настраивается в UI)
- Во время паузы аккаунт не используется в рассылке
- После паузы: автоматический запуск @SpamBot (если включено), затем повторная попытка
- Логирование всех действий с PeerFlood

#### @SpamBot интеграция
- Настройка в UI: "Автозапуск @SpamBot при PeerFlood" (чекбокс)
- Настройка задержки перед запуском @SpamBot (секунды)
- Настройка количества повторных запусков @SpamBot
- Мониторинг ответов от бота и логирование

#### FloodWait улучшения
- Адаптивный расчет пауз: увеличивать интервал между попытками экспоненциально
- Улучшить UI настройки: добавить "Базовая пауза FloodWait" и "Максимальный множитель"
- Логировать все FloodWait события с деталями

#### Возобновление рассылки
- Сохранять состояние: какие сообщения уже отправлены, какие аккаунты активны
- При перезапуске: продолжить с места остановки, пропуская уже отправленные
- UI индикация: "Возобновлено с сообщения N из M"

#### UI настройки антиспама
- Раздел "Антиспам" в расширенных настройках вкладки "Рассылка"
- Настройки PeerFlood:
  - "Пауза при PeerFlood (минуты):" [30]
  - "Автозапуск @SpamBot" [чекбокс]
  - "Задержка перед @SpamBot (сек):" [10]
  - "Максимум запусков @SpamBot:" [3]
- Настройки FloodWait:
  - "Адаптивные паузы" [чекбокс]
  - "Базовая пауза (сек):" [60]
  - "Максимальный множитель:" [5]

### UI/UX

#### Вкладка «Рассылка» - расширенные настройки
```
[▼] Настроить
├── Задержки между волнами...
├── Медиа файлы...
├── Настройки FloodWait...
└── [🆕] Антиспам
    ├── PeerFlood обработка
    │   ├── ☐ Автозапуск @SpamBot при PeerFlood
    │   ├── Задержка перед @SpamBot (сек): [10]
    │   ├── Пауза при PeerFlood (мин): [30]
    │   └── Максимум запусков @SpamBot: [3]
    └── FloodWait улучшения
        ├── ☐ Адаптивные паузы
        ├── Базовая пауза (сек): [60]
        └── Максимальный множитель: [5]
```

#### Логи рассылки
```
12:34:56 Аккаунт1: ❌ PEER_FLOOD → пауза 30 мин, автозапуск @SpamBot...
12:34:57 Аккаунт1: 🤖 @SpamBot запущен, ожидаем снятие ограничений...
12:35:07 Аккаунт1: ✅ @SpamBot: ограничения сняты
12:35:07 Аккаунт1: 🔄 Возобновление отправки через 30 сек...

12:36:45 Аккаунт2: ⏳ FloodWait 120s – адаптивная пауза (x2.0 = 240s)...
12:40:45 Аккаунт2: 🔄 Повтор после адаптивной паузы (120s x2.0)
```

### Интеграционные точки в коде

#### Новый модуль `antispam_manager.py`
```python
class AntiSpamManager:
    def __init__(self, config):
        # Настройки из UI
        self.peerflood_pause_minutes = config.get('antispam', 'peerflood_pause', 30)
        self.spambot_auto_start = config.get('antispam', 'spambot_auto', False)
        self.spambot_delay = config.get('antispam', 'spambot_delay', 10)
        self.spambot_max_tries = config.get('antispam', 'spambot_max_tries', 3)
        self.floodwait_adaptive = config.get('antispam', 'floodwait_adaptive', False)
        self.floodwait_base = config.get('antispam', 'floodwait_base', 60)
        self.floodwait_max_multiplier = config.get('antispam', 'floodwait_max_mult', 5)

        # Состояние аккаунтов
        self.paused_accounts = {}  # account -> {'until': timestamp, 'reason': str}

    def handle_peerflood(self, account_name, client):
        """Обработка PeerFlood: пауза + опционально @SpamBot"""
        pause_until = time.time() + (self.peerflood_pause_minutes * 60)
        self.paused_accounts[account_name] = {
            'until': pause_until,
            'reason': 'PeerFlood'
        }

        if self.spambot_auto_start:
            # Запуск @SpamBot в отдельном потоке
            threading.Thread(
                target=self._run_spambot_check,
                args=(account_name, client),
                daemon=True
            ).start()

    def _run_spambot_check(self, account_name, client):
        """Запуск @SpamBot и проверка снятия ограничений"""
        time.sleep(self.spambot_delay)

        for attempt in range(self.spambot_max_tries):
            try:
                # Отправка команды @SpamBot
                client.send_message('@SpamBot', '/start')
                time.sleep(2)

                # Чтение ответа
                messages = client.get_chat_history('@SpamBot', limit=5)
                # Анализ ответа на наличие ограничений...

                if "ограничений нет" in response_text:
                    # Ограничения сняты - снимаем паузу
                    self.paused_accounts.pop(account_name, None)
                    break

            except Exception as e:
                log(f"Ошибка @SpamBot attempt {attempt+1}: {e}")

            time.sleep(30)  # Задержка между попытками

    def get_adaptive_floodwait(self, account_name, base_wait):
        """Расчет адаптивной паузы FloodWait"""
        if not self.floodwait_adaptive:
            return base_wait

        # Увеличиваем множитель для аккаунта
        multiplier = min(
            getattr(self, f'{account_name}_multiplier', 1.0) * 2.0,
            self.floodwait_max_multiplier
        )
        setattr(self, f'{account_name}_multiplier', multiplier)

        return min(base_wait * multiplier, base_wait * self.floodwait_max_multiplier)

    def is_account_paused(self, account_name):
        """Проверка, находится ли аккаунт в паузе"""
        if account_name in self.paused_accounts:
            pause_info = self.paused_accounts[account_name]
            if time.time() < pause_info['until']:
                return True, pause_info['reason']
            else:
                # Пауза истекла
                del self.paused_accounts[account_name]
                return False, None
        return False, None
```

#### Модификация OptimizedBroadcastWorker
- Добавить AntiSpamManager как зависимость
- Перед отправкой проверять `is_account_paused()`
- При PeerFlood вызывать `handle_peerflood()`
- Для FloodWait использовать `get_adaptive_floodwait()`

#### Состояние рассылки (resume capability)
```python
class BroadcastState:
    def __init__(self, session_id):
        self.session_id = session_id
        self.sent_messages = set()  # (account, recipient) tuples
        self.account_progress = {}  # account -> last_message_index
        self.start_time = time.time()

    def save_progress(self):
        # Сохранение в JSON файл
        pass

    def load_progress(self, session_id):
        # Загрузка из файла
        pass
```

### Обработка ошибок

#### PeerFlood
- 401/402: обработка лицензионных ошибок (уже реализовано в этапе 3)
- 409/422: повтор commit/rollback (уже реализовано)
- Network errors: экспоненциальный backoff

#### @SpamBot
- Если бот не отвечает: продолжить паузу, логировать
- Если бот сообщает об ошибке: увеличить паузу
- Timeout: повторные попытки с увеличенной задержкой

### Тестирование и приёмка (DoD)

#### Позитивное тестирование
- ✅ Успешная обработка PeerFlood: пауза → @SpamBot → возобновление
- ✅ FloodWait с адаптивными паузами: корректное увеличение задержек
- ✅ Возобновление рассылки: продолжение с правильного места
- ✅ Откат лицензии при прерывании (уже протестировано в этапе 3)

#### Негативное тестирование
- ❌ @SpamBot не снимает ограничения: продолжение паузы по таймеру
- ❌ FloodWait > max_multiplier: исключение аккаунта
- ❌ Сеть недоступна: корректное ожидание и повтор
- ❌ Множественные PeerFlood: параллельная обработка аккаунтов

#### Soak-тестирование
- Запуск длительной рассылки (1000+ сообщений)
- Провокация FloodWait/PeerFlood в разных комбинациях
- Валидация, что рассылка завершается корректно
- Проверка потребления памяти/CPU

#### UI/UX валидация
- Все настройки сохраняются и восстанавливаются
- Логи информативны и читаемы
- Нет зависаний UI при паузах
- Корректные подсказки (tooltips)

### Артефакты на выходе

#### Код
- `antispam_manager.py` - новый модуль управления антиспамом
- Обновленный `main.py` с UI настройками и интеграцией
- Модифицированный `OptimizedBroadcastWorker` с антиспам логикой

#### Конфигурация
- Раздел `[antispam]` в `settings.ini`
- Значения по умолчанию для всех настроек

#### Документация
- Обновленный `README.md` с разделом "Антиспам"
- Таблица ошибок и стратегий обработки

#### Тесты
- Короткий отчет о soak-тестировании
- Скриншоты UI настроек
- Фрагменты логов с примерами обработки ошибок

### Примечания

- **PeerFlood стратегия**: жесткая пауза обязательна, @SpamBot - опционально
- **Адаптивные паузы**: не заменяют, а дополняют существующие FloodWait настройки
- **Resume**: сохранять состояние в `broadcast_sessions/{session_id}.json`
- **Безопасность**: @SpamBot запускается только при явном согласии пользователя
- **Мониторинг**: все действия логируются для анализа эффективности

### Риски и смягчение

- **@SpamBot не работает**: пауза по таймеру как fallback
- **Частые PeerFlood**: мониторинг, рекомендации пользователю по cooldown
- **Memory leaks**: ограничение размера состояния рассылки
- **UI freezes**: все тяжелые операции в отдельные потоки
